/*function needsLicense(kind) {
    return kind === 'car' || kind === 'truck';
}

console.log(needsLicense('car'));*/


/*function chooseVehicle(option1, option2) {
    const compareResult =  option1.localeCompare(option2)

    console.log(compareResult);

    if ( compareResult < 0) {
        return `${option1} is clearly the better choice.`
    } else if (compareResult > 0) {
        return `${option2} is clearly the better choice`
    } else {
        return 'Both options are good enough. You can choose either.'
    }
}

console.log(chooseVehicle('Volkswagen Beetle', 'Volkswagen Golf'));*/


/*function calculateResellPrice(originalPrice, age) {
    if (age <3) {
        return Math.round(0.8 * originalPrice)
    } else if ( age <= 10) {
        return Math.round(0.7 * originalPrice)
    } else {
        return Math.round(0.5 * originalPrice)
    }
}*/

